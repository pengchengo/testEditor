using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LPRunEdge
{
    public LpRunNode sourceNode = null;
    public LpRunPort sourcePort = null;
    public LpRunNode targetNode = null;
    public LpRunPort targetPort = null;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
